version: '3.8'
services:
  mongodb:
    image: mongo
    volumes:
      - data:/data/db
  booking:
    build: ./Microservices/BookingService
    ports:
      - '5000:5000'
    env_file:
      - ./env
    depends_on:
      - mongodb
  app:
    build: ./MainApp
    ports:
      - '5001:5001'
    env_file:
      - ./env
    depends_on:
      - booking
volumes:
  data:
        