version: '3.8'
services:
  mongodb:
    image: mongo
    volumes:
      - data:/data/db
  booking:
    build: ./MicroServices/BookingService
    ports:
      - '5000:5000'
    volumes:
      - ./MicroServices/BookingService:/app
    env_file:
      - ./env
    depends_on:
      - mongodb
  app:
    build: ./MainApp
    ports:
      - '5001:5001'
    volumes:
      - ./MainApp:/app
    env_file:
      - ./env
    depends_on:
      - booking
volumes:
  data:
        
